{# templates/blog/category.html.twig #}
{% extends 'base.html.twig' %}

 {% block body %}
     <p>Nom de la catégorie des articles: {{ category.name }}</p>
     {% for article in articles %}

         <h1>{{ article.id }}: {{ article.title }}</h1><br>
         <p>{{ article.content }}</p><br>

     {% endfor %}

 {% endblock %}